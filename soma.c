#include <stdio.h>

int main() {
    int a, b, soma;
    printf("Digite dois numeros: ");
    scanf("%d %d", &a, &b);

    soma = a + b; // processamento

    printf("A soma e: %d\n", soma);
    return 0;
}
